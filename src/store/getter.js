import state from './state'
export const user = (user) => state.user
export const types = (types) => state.types
export const title = title => state.title
export const showBack = (showBack) => state.showBack
export const showMore = (showMore) => state.showMore
export const showBottom = (showBottom) => state.showBottom
export const showTitle = (showTitle) => state.showTitle
